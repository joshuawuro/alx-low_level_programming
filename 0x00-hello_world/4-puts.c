#include <stdio.h>
/**
 * main - outputs the message
 *
 * return 0 - sucess
 */
int main(void)
{
	char message[] = "\"Programming is like building a multilingual puzzle";

	puts(message);
	return (0);
}
