#include <stdio.h>
/**
 * main - prints the message to the stdio stream
 *
 * Return: 0 success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
